# Transient files
*.lock
!Cargo.lock
*.tmp
*.bak

# Rust build output
/target

# Local assistant and workspace state (never shared)
/.cs/
/.ws/

# Claude Code local settings (recreated on each machine)
.claude/settings.local.json

# Machine-local session protocol
CLAUDE.local.md

# OS files
.DS_Store
Thumbs.db

# Editor and tool config
.vscode/
.idea/
.obsidian/
*.swp
*.swo
*~

# Signing keys must never be committed. The public key belongs in install.sh;
# the secret key belongs in GitHub Actions secrets and an offline backup only.
# See docs/releasing.md.
minisign.key
*.key
*.pem
